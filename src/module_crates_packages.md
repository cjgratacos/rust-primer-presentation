# Module, Crates and Packages

- Package is your project.
- Crates are dependencies.
- Modules are internal packages inside your project structure.
- Use project to present `modules`, `crates` and `packages`.
